<h2>Dashboard</h2>

<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 20px; margin-top: 20px;">
    
    <div style="background: #f8f9fa; padding: 20px; border-radius: 8px; border-left: 4px solid #d40511;">
        <h3>Warehouse Sanitation Checklists</h3>
        <p>Access and manage sanitation checklists for your facility.</p>
        <a href="/app" class="btn">Open Checklist App</a>
    </div>
    
    <div style="background: #f8f9fa; padding: 20px; border-radius: 8px; border-left: 4px solid #28a745;">
        <h3>User Profile</h3>
        <p><strong>Username:</strong> <%= user.username %></p>
        <p><strong>Role:</strong> <%= user.role || 'User' %></p>
        <% if (user.isAdmin) { %>
            <p><strong>Admin Access:</strong> Yes</p>
        <% } %>
    </div>
    
    <% if (user.role === 'admin' || user.isAdmin) { %>
    <div style="background: #f8f9fa; padding: 20px; border-radius: 8px; border-left: 4px solid #ffc107;">
        <h3>Admin Panel</h3>
        <p>Manage users, view reports, and system administration.</p>
        <a href="/admin" class="btn">Admin Dashboard</a>
    </div>
    <% } %>
    
    <% if (user.role === 'manager' || user.role === 'admin' || user.isAdmin) { %>
    <div style="background: #f8f9fa; padding: 20px; border-radius: 8px; border-left: 4px solid #17a2b8;">
        <h3>Manager Tools</h3>
        <p>Access manager-specific tools and reports.</p>
        <a href="/manager" class="btn">Manager Dashboard</a>
    </div>
    <% } %>
    
    <% if (user.role === 'compliance' || user.role === 'admin' || user.isAdmin) { %>
    <div style="background: #f8f9fa; padding: 20px; border-radius: 8px; border-left: 4px solid #6f42c1;">
        <h3>Compliance</h3>
        <p>View compliance reports and audit trails.</p>
        <a href="/compliance" class="btn">Compliance Dashboard</a>
    </div>
    <% } %>
    
</div>

<div style="margin-top: 30px;">
    <h3>Quick Actions</h3>
    <div style="display: flex; gap: 10px; flex-wrap: wrap;">
        <a href="/app" class="btn">New Checklist</a>
        <a href="/checklists" class="btn">View Submissions</a>
        <a href="/logout-page" class="btn" style="background-color: #6c757d;">Logout</a>
    </div>
</div>

<div style="margin-top: 30px; padding: 20px; background: #e9ecef; border-radius: 8px;">
    <h4>System Information</h4>
    <p><strong>Application:</strong> DHL Sanitation Management System</p>
    <p><strong>Version:</strong> 1.0.2</p>
    <p><strong>Environment:</strong> Development</p>
    <p><strong>Login Time:</strong> <%= new Date().toLocaleString() %></p>
</div>
